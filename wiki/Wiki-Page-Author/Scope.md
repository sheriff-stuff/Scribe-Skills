# Scope

The skill handles two distinct regions of a page, each with its own rule set:

- The page **body** — everything outside `> [!ODD]` callout blocks. See [Body Rules](Body-Rules).
- `> [!ODD]` callout blocks — rules are inverted from the body. See [Open Design Decision](Open-Design-Decision).

# Scope

The skill handles two distinct regions of a page, each with its own rule set:

- The page **body** — everything except `## Open design decisions`. See [Body Rules](Body-Rules).
- The `## Open design decisions` section — rules are inverted from the body. See [Open Design Decisions Rules](Open-Design-Decisions-Rules).
